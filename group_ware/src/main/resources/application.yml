# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: group_ware
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://127.0.0.1:3306/group_ware
    username: scott
    password: tiger
  security:
    user:
      name: user
      password: 1234
server:
  port: 8095
 